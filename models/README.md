# Models

개인 실험 모델은 Git에 올리지 않습니다. 최종 선정 후 재학습한
`final_model.joblib`만 제출 대상으로 관리합니다.

구조 변경 전에 이미 커밋됐던 김예찬의 두 모델은 재현 근거 보존을 위해
`experiments/kim_yechan/artifacts/models/`로 이동했습니다. 이후 생성하는 개인
모델은 `.gitignore` 대상입니다.
